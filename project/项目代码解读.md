# 明眸智签 - 项目代码解读

## 📁 文件结构说明

### 核心文件

| 文件 | 说明 |
|------|------|
| `app.py` | Flask后端主应用，包含所有路由、数据库模型、API接口、数据库迁移逻辑 |
| `face_recognition_backend.py` | Python 后端人脸识别模块（InsightFace + ONNX Runtime）+ LivenessTracker 活体检测 |
| `templates/base.html` | 基础模板，包含顶栏导航和全局赛博极简设计系统 |
| `templates/login.html` | 教师登录页面（独立设计系统） |
| `templates/register.html` | 教师注册页面 |
| `templates/classes.html` | 班级管理页面（含Excel导入导出） |
| `templates/students.html` | 学生管理页面（含Excel导入导出和人脸采集） |
| `templates/capture_face.html` | 人脸采集页面（双特征：128维 + 512维） |
| `templates/attendance.html` | 人脸识别签到页面（核心，含活体检测和前后端双模式） |
| `templates/batch_attendance.html` | 批量人脸识别签到页面 |
| `templates/qrcode_attendance.html` | 二维码签到教师端（动态二维码+待确认列表） |
| `templates/qr_signin.html` | 二维码签到学生端（扫码+学号验证） |
| `templates/courses.html` | 课程管理页面 |
| `templates/records.html` | 签到记录查询页面 |
| `templates/statistics.html` | 数据统计页面（Canvas图表） |
| `templates/super_admin.html` | 超级管理员后台 |
| `static/face-api.js` | 前端人脸识别库 |
| `static/models/` | 前端 face-api.js 模型文件 |
| `.insightface/models/buffalo_l/` | 后端 InsightFace ONNX 模型文件 |
| `requirements.txt` | Python依赖 |

---

## 🔧 app.py 核心代码解读

### 1. 导入和初始化

```python
import os
import re
from datetime import datetime, date, timedelta
from flask import Flask, render_template, request, redirect, url_for, jsonify, send_file, session
from flask_sqlalchemy import SQLAlchemy
from werkzeug.security import generate_password_hash, check_password_hash
from functools import wraps
import numpy as np
import openpyxl
import qrcode

from face_recognition_backend import get_recognizer, get_liveness_tracker
```

**说明：**
- `Flask`：Web框架
- `SQLAlchemy`：ORM数据库操作库
- `numpy`：人脸特征距离计算
- `qrcode`：二维码生成
- `insightface`：后端人脸识别库
- `get_liveness_tracker()`：新增的活体检测单例

### 2. 数据库模型定义

#### User（用户/教师）
```python
class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(50), unique=True, nullable=False)
    name = db.Column(db.String(100))
    password_hash = db.Column(db.String(200), nullable=False)
    role = db.Column(db.String(20), default='teacher')  # teacher/super_admin
    created_at = db.Column(db.DateTime, default=datetime.now)
```

#### Class（班级）
```python
class Class(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    grade = db.Column(db.String(50))
    description = db.Column(db.Text)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    created_at = db.Column(db.DateTime, default=datetime.now)
```

#### Student（学生）
```python
class Student(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    student_id = db.Column(db.String(50), nullable=False)
    class_id = db.Column(db.Integer, db.ForeignKey('class.id'), nullable=False)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    face_descriptor = db.Column(db.LargeBinary)  # 128维人脸特征（前端模式）
    face_descriptor_512 = db.Column(db.LargeBinary)  # 512维人脸特征（后端模式）⭐
    created_at = db.Column(db.DateTime, default=datetime.now)
```

#### Course（课程）
```python
class Course(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    code = db.Column(db.String(50))
    description = db.Column(db.Text)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    classes = db.relationship('Class', secondary='course_class', backref='courses')
```

#### Attendance（签到记录）
```python
class Attendance(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    student_id = db.Column(db.Integer, db.ForeignKey('student.id'), nullable=False)
    class_id = db.Column(db.Integer, db.ForeignKey('class.id'), nullable=False)
    course_id = db.Column(db.Integer, db.ForeignKey('course.id'))
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    check_in_time = db.Column(db.DateTime, default=datetime.now)
    method = db.Column(db.String(20), default='face')  # face/manual/qrcode
```

#### PendingAttendance（二维码待确认签到）
```python
class PendingAttendance(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    student_id = db.Column(db.Integer, db.ForeignKey('student.id'), nullable=False)
    class_id = db.Column(db.Integer, db.ForeignKey('class.id'), nullable=False)
    course_id = db.Column(db.Integer, db.ForeignKey('course.id'), nullable=False)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    request_time = db.Column(db.DateTime, default=datetime.now)
    status = db.Column(db.String(20), default='pending')  # pending/confirmed/rejected
    method = db.Column(db.String(20), default='qrcode')
```

**说明：**
- `LargeBinary`：存储二进制数据（人脸特征向量）
- `db.relationship`：定义模型关联关系
- 所有核心模型都通过 `user_id` 实现多租户数据隔离
- `face_descriptor_512` 是新增字段，用于存储 InsightFace 512维特征

### 3. 数据库自动迁移 ⭐

```python
with app.app_context():
    db.create_all()
    
    # 数据库迁移：为已有数据库添加 face_descriptor_512 列
    from sqlalchemy import inspect, text
    inspector = inspect(db.engine)
    columns = [c['name'] for c in inspector.get_columns('student')]
    if 'face_descriptor_512' not in columns:
        with db.engine.connect() as conn:
            conn.execute(text('ALTER TABLE student ADD COLUMN face_descriptor_512 BLOB'))
            conn.commit()
        print('[DB] 已添加 face_descriptor_512 列')
```

### 4. 登录与权限

```python
@app.route('/login', methods=['GET', 'POST'])
def login():
    if request.method == 'POST':
        username = request.form.get('username', '').strip()
        password = request.form.get('password', '')
        
        if not username or not password:
            return render_template('login.html', error='请输入用户名和密码')
        
        user = User.query.filter_by(username=username).first()
        
        if user and check_password_hash(user.password_hash, password):
            session.clear()
            session['user_id'] = user.id
            session['username'] = user.username
            session['name'] = user.name
            session['role'] = user.role
            if user.role == 'super_admin':
                return redirect(url_for('super_admin_dashboard'))
            return redirect(url_for('index'))
        else:
            return render_template('login.html', error='用户名或密码错误')
    
    return render_template('login.html')
```

**说明：**
- 登录前先 `session.clear()` 防止会话固定攻击
- 使用 `check_password_hash` 验证密码（不再硬编码）
- 超级管理员和普通教师通过 `role` 字段区分路由

### 5. 双特征人脸保存 API ⭐ ⭐

```python
@app.route('/api/save_face', methods=['POST'])
def save_face():
    data = request.json
    student_id = data['student_id']
    descriptor = np.array(data['descriptor'], dtype=np.float32)
    image_b64 = data.get('image')
    
    student = Student.query.get(student_id)
    student.face_descriptor = (descriptor / (np.linalg.norm(descriptor) + 1e-8)).tobytes()
    
    has_512 = False
    if image_b64:
        try:
            recognizer = get_recognizer()
            faces = recognizer.detect_faces(image_b64)
            if faces:
                descriptor_512 = faces[0].get('embedding')
                if descriptor_512 is not None:
                    student.face_descriptor_512 = descriptor_512.astype(np.float32).tobytes()
                    has_512 = True
        except Exception as e:
            print(f'[SaveFace] 512维特征采集失败: {e}')
    
    db.session.commit()
    msg = '人脸采集成功（128维+512维双特征）' if has_512 else '人脸采集成功（仅128维，后端识别不可用）'
    print(f'[SaveFace] {msg}')
    return jsonify({'success': True, 'message': msg, 'has_512': has_512})
```

### 6. 后端人脸识别 API（含活体检测）⭐ ⭐

```python
@app.route('/api/recognize_frame', methods=['POST'])
def recognize_frame():
    data = request.json
    class_id = data['class_id']
    image_b64 = data['image']
    
    user = get_current_user()
    students = Student.query.filter_by(class_id=class_id, user_id=user.id).all()
    students_dict = {s.id: s.face_descriptor_512 for s in students}
    
    recognizer = get_recognizer()
    result = recognizer.recognize(image_b64, students_dict)
    recognized = result['recognized']
    face_count = result['detected_faces']
    liveness_ok = result.get('liveness', False)
    nose_frames = result.get('nose_frames', 0)
    
    print(f'[RecognizeFrame] 检测到 {face_count} 张人脸，匹配 {len(recognized)} 人，活体={liveness_ok} ({nose_frames}帧)')
```

### 7. 活体重置 API ⭐

```python
@app.route('/api/reset_liveness', methods=['POST'])
def reset_liveness():
    if 'user_id' not in session:
        return jsonify({'success': False, 'error': '未登录'})
    get_liveness_tracker().reset()
    print('[Liveness] 活体追踪器已重置')
    return jsonify({'success': True})
```

---

## 🔧 face_recognition_backend.py 核心代码解读

### 1. LivenessTracker 活体检测类 ⭐ ⭐

```python
class LivenessTracker:
    def __init__(self):
        self.nose_history = []
        self.max_history = 25
        self.min_frames = 6
        self.movement_threshold = 10

    def update(self, nose_tip):
        self.nose_history.append({
            'x': float(nose_tip[0]),
            'y': float(nose_tip[1]),
            'time': time.time()
        })
        if len(self.nose_history) > self.max_history:
            self.nose_history = self.nose_history[-self.max_history:]

    def is_live(self):
        if len(self.nose_history) < self.min_frames:
            return False
        recent = self.nose_history[-self.min_frames:]
        y_values = [p['y'] for p in recent]
        x_values = [p['x'] for p in recent]
        y_range = max(y_values) - min(y_values)
        x_range = max(x_values) - min(x_values)
        return y_range > self.movement_threshold or x_range > self.movement_threshold

    def get_nose_count(self):
        return len(self.nose_history)

    def reset(self):
        self.nose_history = []

_liveness_tracker = LivenessTracker()

def get_liveness_tracker():
    return _liveness_tracker
```

### 2. FaceRecognizer 核心类

```python
from face_recognition_backend import get_recognizer

class FaceRecognizer:
    def __init__(self, use_gpu=False):
        self.use_gpu = use_gpu
        self.use_real_models = False
        self.embedding_dim = 512  # InsightFace 模式 512维
        self._init_insightface()
```

**核心功能：**
- **单例模式**：通过 `get_recognizer()` 全局复用，避免重复加载模型
- **双模式支持**：InsightFace 真实模型 或 模拟降级模式
- **活体检测集成**：通过 `LivenessTracker` 跟踪鼻尖移动

### 3. 检测 + 关键点提取

```python
def _detect_faces_insightface(self, img):
    faces = self.analysis.get(img)
    print(f'[FaceRecognizer] 检测到 {len(faces)} 张人脸')
    result = []
    for face in faces:
        bbox = face.bbox.astype(int).tolist()
        print(f'[FaceRecognizer]   人脸 bbox={bbox} score={face.det_score:.2f}')
        embedding = face.embedding
        embedding = embedding / (np.linalg.norm(embedding) + 1e-8)
        nose_tip = None
        if hasattr(face, 'kps') and face.kps is not None and len(face.kps) >= 3:
            nose_tip = face.kps[2].tolist()
        result.append({
            'bbox': bbox,
            'confidence': float(face.det_score),
            'embedding': embedding,
            'nose_tip': nose_tip  # 新增：鼻尖关键点
        })
    return result
```

### 4. 余弦相似度匹配 ⭐

```python
for student_id, stored_desc in students_dict.items():
    if stored_desc is None:
        continue
    if not self._check_dimension_match(stored_desc, current_dim):
        continue
    stored = np.frombuffer(stored_desc, dtype=np.float32)
    stored = stored / (np.linalg.norm(stored) + 1e-8)
    cos_sim = float(np.dot(descriptor, stored))
    distance = float(np.linalg.norm(descriptor - stored))
    print(f'[Recognize] student_id={student_id} cos_sim={cos_sim:.4f} distance={distance:.4f}')
    
    if cos_sim > 0.45 and cos_sim > max_cos_sim:
        max_cos_sim = cos_sim
        best_match = student_id
```

### 5. 活体检测集成

```python
tracker = get_liveness_tracker()
liveness_ok = False
nose_frames = 0

if faces and faces[0].get('nose_tip'):
    tracker.update(faces[0]['nose_tip'])
    nose_frames = tracker.get_nose_count()
    liveness_ok = tracker.is_live()
    print(f'[Recognize] 活体检测: nose_frames={nose_frames}, liveness={liveness_ok}')

if best_match is not None:
    if not liveness_ok:
        print(f'[Recognize] 匹配到 student_id={best_match} 但活体检测未通过')
        continue
```

---

## 🎨 前端设计系统

### 设计令牌（CSS变量）

```css
:root {
    --brand: #00ff88;        /* 青霓色 */
    --bg-deep: #050510;      /* 深空背景 */
    --bg-surface: #0a1210;   /* 表面层 */
    --text-primary: #f0f2f5;
    --border: rgba(0, 255, 136, 0.12);
    --ease-pulse: cubic-bezier(0.16, 1, 0.3, 1);
}
```

### 动效体系
- **入场动画**：内容区从 `translateY(12px)` 淡入
- **按钮反馈**：`:active` 时 `scale(0.96)`，悬停时 `translateY(-2px)`
- **学生列表**：stagger 入场，每行延迟 `i * 0.04s`
- **签到成功**：绿光涟漪扩散 + 卡片弹入 + 摄像头闪光
- **无障碍**：全局 `@media (prefers-reduced-motion)` 降级

---

## 📊 数据流转图

### 人脸识别签到流程（双模式）

```
前端模式: 摄像头 → face-api.js检测+点头验证 → 提取128维特征 → /api/recognize_face → 归一化+欧氏距离匹配 → 签到

后端模式: 摄像头 → Canvas截图(Base64) → /api/recognize_frame → 
    1. InsightFace检测+提取512维特征
    2. LivenessTracker跟踪鼻尖移动
    3. 活体检测通过才开始匹配
    4. 余弦相似度匹配 → 签到
```

### 人脸采集流程（双特征）

```
摄像头 → face-api.js检测 → 提取128维特征 → 
同时截图为JPEG Base64 → /api/save_face → 
    1. 保存128维特征（归一化）
    2. InsightFace重新提取512维特征（归一化）
    3. 两个特征都保存到数据库
```

---

## 💡 开发建议

### 1. 先跑通现有代码
在开始修改之前，先确保能完整运行现有项目

### 2. 小步快跑
每次只修改一个小功能，测试通过后再继续

### 3. 多打印日志
在关键位置通过 `showDebug()` 或 `print()` 帮助调试

### 4. 使用调试功能
- 签到页面右上角"调试"按钮
- 浏览器开发者工具（F12）
- Python调试器（pdb）

### 5. 版本控制
每次完成一个功能，做一次Git提交，方便回退
